package com.example.string_events;
import android.app.Application;

public class NotificationServiceHelper extends Application{

    // static flag used to check whether the notification service has already been started
    public static boolean isNotificationServiceRunning = false;
}
